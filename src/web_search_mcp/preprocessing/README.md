# Preprocessing

Optional model-assisted transformations live here. They must remain bounded,
fail open to deterministic behavior, treat retrieved text as untrusted input,
and never replace extraction, retrieval, reranking, or durable evidence.

See [`docs/preprocessing-lfm.md`](../../../docs/preprocessing-lfm.md).
